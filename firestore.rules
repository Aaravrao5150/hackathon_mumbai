rules_version = '2';
service cloud.firestore {
 match /databases/{db}/documents {

  match /attendance/{id}{
   allow read,write: if request.auth!=null;
  }

  match /sessions/{id}{
   allow read: if request.auth!=null;
   allow write: if request.auth.token.email.matches('teacher.*');
  }
 }
}
